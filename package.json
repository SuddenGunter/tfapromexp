{
  "name": "tfapromexp",
  "version": "1.0.0",
  "description": "TFA Prometheus Exporter - CO2 Monitor metrics exporter for Prometheus",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t tfapromexp .",
    "docker:compose": "docker-compose up",
    "lint": "echo \"No linter configured\" && exit 0"
  },
  "keywords": [
    "prometheus",
    "exporter",
    "co2",
    "monitor",
    "tfa",
    "metrics"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "co2-monitor": "^3.0.0",
    "express": "^4.21.2",
    "prom-client": "^15.1.3"
  }
}
